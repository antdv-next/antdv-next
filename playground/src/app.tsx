import { Tabs } from 'antdv-next'
import { defineComponent } from 'vue'

export const A = defineComponent(() => {
  return () => {
    return (
      <Tabs
        onChange={() => {

        }}
      >
      </Tabs>
    )
  }
})
